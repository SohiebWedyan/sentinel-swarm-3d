"""Swarm Communication Layer (Phase 5+).

MQTT topics/config already exist (config/mqtt.yaml) so the interface below
can be exercised against a real broker as soon as Phase 5 starts, without
revisiting the topic design later phases (formation control, map fusion)
already depend on.
"""
